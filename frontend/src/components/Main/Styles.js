import { makeStyles } from '@material-ui/core/styles';

export default makeStyles( (theme) => ({
    
    siteName: {
        textAlign: "center",
        paddingTop: '70px',
        height: '50px',
      },
    
    sns: {
        gridRow: "2/3",
        textAlign: 'left',
        padding: '10px',
    }

}) );
